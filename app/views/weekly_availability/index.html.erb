<weekly-availability
  :services="<%= @services.as_json(include: :contract_schedules).to_json %>"
  :weeks="<%= @weeks.to_json %>"
  :engineers="<%= @engineers.to_json %>"
></weekly-availability>
<% #= @services.as_json(include: :contract_schedules).to_json %>
<% #= @weeks.to_json %>
<% #= @engineers.to_json %>
