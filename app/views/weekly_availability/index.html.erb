<weekly-availability
  :services="<%= @services.to_json %>"
  :weeks="<%= @weeks.to_json %>"
  :engineers="<%= @engineers.to_json %>"
></weekly-availability>
<% #= @services.to_json %>
<% #= @weeks.to_json %>
<% #= @engineers.to_json %>
